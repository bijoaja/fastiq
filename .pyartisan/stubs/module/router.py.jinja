import uuid
from fastapi import APIRouter, Depends, Query
from app.core.responses import ApiResponse, ApiListResponse
from app.core.pagination import build_pagination_info
from app.modules.{{name_lower}}.schemas import Create{{name_title}}Request, {{name_title}}Response
from app.modules.{{name_lower}}.service import {{name_title}}Service

# Router prefix will be set dynamically via include_router in main.py
# If you prefer to expose prefix directly, set prefix="/{{name_plural}}"
router = APIRouter(prefix="/{{name_plural}}", tags=["{{name_title}}"])


@router.post("/", response_model=ApiResponse[{{name_title}}Response], status_code=201)
async def create_{{name_lower}}(
    request: Create{{name_title}}Request,
    # service: {{name_title}}Service = Depends(get_{{name_lower}}_service)  # Uncomment after defining in core/dependencies.py
) -> ApiResponse[{{name_title}}Response]:
    # TODO: Implement service logic call
    # response_data = {{name_title}}Response.model_validate(result)
    # return ApiResponse(data=response_data)
    pass


@router.get("/{id}", response_model=ApiResponse[{{name_title}}Response])
async def get_{{name_lower}}(
    id: uuid.UUID,
) -> ApiResponse[{{name_title}}Response]:
    # TODO: Implement service logic call
    pass
